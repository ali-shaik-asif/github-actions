name: push Demo # name of the workflow 

# This defines the event that triggers the workflow
on: 
  push:
    branches:
      - 'feature*'
#workflow_dispatch # triggeres 

jobs:
  say-hello-from-triggers-push:
    # The type of virtual machine the job will run on
    runs-on: ubuntu-latest

    steps:
      # Step 1: Print the classic message in the logs
       - name: push 
         run: echo "Hello, World!,from push "

       - name: feature branch push 
         run: echo ""
      